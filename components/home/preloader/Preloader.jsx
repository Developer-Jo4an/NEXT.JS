import Spinner from '@/components/general/Spinner'

const Preloader = () => {
    return (
        <div className={'preloader page'}>
            <Spinner />
        </div>
    )
}

export default Preloader